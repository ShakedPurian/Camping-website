<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <script src='https://api.mapbox.com/mapbox-gl-js/v2.7.0/mapbox-gl.js'></script>
    <link href='https://api.mapbox.com/mapbox-gl-js/v2.7.0/mapbox-gl.css' rel='stylesheet' />
    
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/phoneStyle.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Assistant:wght@400;600;800&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Caveat:wght@700&display=swap" rel="stylesheet">

    <title>CampIL</title>
</head>
<body>
  <div class="nav">
    <div class="nav-links">
      <a class="campil" href="/">CampIL</a>
      <a class="nav-link" href="/campgrounds">Campgrounds</a>
      <a class="nav-link" id="nav-add" href="/campgrounds/new">Add a camp</a>
    </div>
    <div class="nav-user">
      <% if (!currentUser) { %> 
      <a class="nav-link" href="/campgrounds/register">Register</a>
      <a class="nav-link" href="/campgrounds/login">Log In</a>
      <% } else { %> 
      <a class="nav-link" href="/logout">Log out</a>
      <% } %> 
    </div>
  </div>

<main>

  <% if (success && success.length) { %>
   <div class="message"><%= success %></div> 
  <% } %>
  <% if (error && error.length) { %>
    <div class="message"><%= error %> </div>
  <% } %>
<%- body %>

</main>
<footer>
  <div class="nav">
    <a href="/">Home</a>
    <a href="/contact">Contact Us</a>
  </div>
</footer>
</body>
</html>